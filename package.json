{
  "name": "dameons-portfolio",
  "version": "1.0.0",
  "description": "",
  "workspaces": [
    "back",
    "front"
  ],
  "scripts": {
    "build": "concurrently \"npm run build --workspace back\" \"npm run build --workspace front\"",
    "watch": "concurrently \"npm run watch --workspace back\" \"npm run watch --workspace front\"",
    "dev": "concurrently \"npm run dev --workspace back\" \"npm run dev --workspace front\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.6.0",
    "nodemon": "^2.0.20"
  }
}
